

			//Once we've completed a cue frame, advance through the following scenes until we get
			// to something the renderer can output meaningfully. This ensures transitions between
			// states happens smoothly, since we will have contiguous cue frames bookending the
			// scripts.
//			do{
//				goToNextScene();
//				scriptGameFrame();
//			} while(getScene().type != SCENE_CUE);
